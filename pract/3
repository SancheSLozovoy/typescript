; Описать функцию словами, рассказать для чего в ней нужны дженерики

; 1 функция

; function identity<T>(arg: T): T

; 2 функция

; class GenericNumber<T> {
;     zeroValue: T;
;     add: (x: T, y: T) => T;
; }
 
; let myGenericNumber = new GenericNumber<number>();

1. Функция принимает один аргумент типа T и возвращает этот же тип. Тип T является универсальным, то есть функция может работать с любым типом данных, используя дженерики, 
без необходимости создавать несколько перегруженных версий функции для разных типов.


2 . как и в первом случае, дженерики позволяют классу работать с данными любого типа, подстраиваясь под конкретный тип при использовании.

